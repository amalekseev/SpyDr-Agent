from pydantic_settings import BaseSettings, SettingsConfigDict

class Config(BaseSettings):
    OPENAI_API_KEY: str = ""
    MODEL_NAME: str = "gpt-4.1-mini"
    TEMPERATURE: float = 0.7
    GENERATIONS_PER_FEATURE: int = 1
    
    SYSTEM_PROMPT: str = (
        "Ты опытный QA-инженер, который пишет ручные тест-кейсы для тестировщиков. "
        "Тестировщик, который будет выполнять твой тест, НЕ видит исходный автотест и не знает Gherkin. "
        "Он работает с реальной системой через пользовательский интерфейс, API-клиент (Postman/Swagger) или консоль администратора."
    )

    PROMPT_TEMPLATE: str = """Ниже приведен автоматизированный тест (Gherkin feature). Твоя задача — написать на его основе ручной тест-кейс, который тестировщик сможет выполнить вручную.

ВАЖНЫЕ ПРАВИЛА:
1. НЕ копируй шаги автотеста дословно. Переосмысли их: опиши действия так, как их выполнил бы живой человек через интерфейс или инструмент.
2. Замени технические артефакты автоматизации на понятные человеку действия:
   - Вместо "Присвоить переменной X значение Y" -> опиши какие данные подготовить и какие значения ввести.
   - Вместо "Отправить сообщение в Kafka в топик X" -> опиши действие в бизнес-терминах (например: "Инициировать загрузку счета через входной канал").
   - Вместо SQL-запросов -> опиши что именно нужно проверить и где (например: "Убедиться, что в системе появилась запись о счете с корректными реквизитами").
   - Вместо "код ответа 200" -> "Запрос выполнен успешно" или "Операция завершена без ошибок".
   - Вместо переменных вида ${{variable}} -> используй конкретные описания (например: "ранее созданный идентификатор счета").
3. Структура каждого тест-кейса:
   Название теста: <краткое понятное название>
   Предусловия: <что должно быть настроено или подготовлено заранее>
   Шаги: <пронумерованные действия тестировщика>
   Ожидаемый результат: <что должно произойти после выполнения шагов>
4. Если в feature несколько Scenario — создай отдельный тест-кейс для каждого.
5. Язык: строго русский.
6. Формат: простой текст (plain text). ЗАПРЕЩЕНО использовать Markdown-разметку (**, ##, - списки и т.д.).
7. Верни ТОЛЬКО текст тест-кейсов, без вступлений, комментариев и пояснений.

Gherkin Feature:
{gherkin_content}

Ручные тест-кейсы:
"""

    model_config = SettingsConfigDict(
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore"
    )

# Default configuration instance
config = Config()
